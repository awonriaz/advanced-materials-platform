persistence false
log_dest stdout
listener 1883 0.0.0.0
allow_anonymous true
# Demo broker is bound to 127.0.0.1 by docker-compose.
# Production: set allow_anonymous false, enable TLS, and use per-device credentials.
